<a id="home" href="/">Home</a>

<h2>These are your interests <span class="member-name"></span></h2>

<ul>
  {{#each allChosen}}
  	<li data-id={{InterestId}} id="{{InterestId}}">
          <a href="/userinterest/{{InterestId}}">{{interestName}}</a>
  	</li>
    <br>
  {{/each}}
</ul>
